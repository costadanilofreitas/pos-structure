import PropTypes from 'prop-types'

const WorkingModePropTypes = PropTypes.shape({
  podType: PropTypes.string,
  posFunction: PropTypes.string,
  usrCtrlType: PropTypes.string
})

export default WorkingModePropTypes
